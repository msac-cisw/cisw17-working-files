// variables.js

/*
* https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Grammar_and_types#variables
* A JavaScript identifier usually starts with a letter, underscore (_), or dollar sign ($). Subsequent characters can also be digits (0 – 9). Because JavaScript is case sensitive, letters include the characters A through Z (uppercase) as well as a through z (lowercase).
*/

// Assignment operators

/*
=   // let first_name = "John";
+=  // let age = 30; age += 1; // age is now 31
-=  // let score = 100; score -= 10; // score is now 90
++  // let count = 0; count++; // count is now 1
-- // let count = 1; count--; // count is now 0
%  // let remainder = 10 % 3; // remainder is now 1 (modulus)
/  // let quotient = 10 / 2; // quotient is now 5
*
+ (add)
+ (concatenate)
*/