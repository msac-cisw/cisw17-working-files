// Count down to 1

// Declare variable for the count
let count;

// Use a for loop to count down
// for () {};
for (
    count = 10;
    count >= 1;
    count = count -1
) {
    console.log(count, "🍪");
};
console.log("Blast off! 🚀🍦🍨🍧")
